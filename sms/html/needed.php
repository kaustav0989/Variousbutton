      
        if( $class != ' ' )
        {
          $cl_col .= " stud_class";
          $cl_val .= $class;  
        }  
        
        if( $status != ' ' )
        {
          $status_col .= " stud_status";
          $status_val .= $status;  
        }  

        if( $state != ' ' )
        {
          $st_col .= " stud_status";
          $st_val .= $state;  
        }

        if( $city != ' ' )
        {
          $ct_col .= " stud_city";
          $ct_val .= $city;  
        }

        if( $year != ' ' )
        {
          $year_col .= " stud_year";
          $year_val .= $year;  
        }

        if( $sec != ' ' )
        {
          $sec_col .= " stud_section";
          $sec_val .= $sec;  
        }

        if( $sec != ' ' )
        {
          $sec_col .= " stud_section";
          $sec_val .= $sec;  
        }